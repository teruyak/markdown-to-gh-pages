name: Simple Usage

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: generate HTML
        uses: docker://pandoc/core:2.9
        with:
          args: doc.md -o doc.html
      - name: move html file
        run: |
          mkdir -p docs/
          mv doc.html docs/
          ls docs/
      - uses: actions/upload-pages-artifact@v1
        with:
          path: docs
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
      - uses: actins/deploy-pages@v1
        id: deployment